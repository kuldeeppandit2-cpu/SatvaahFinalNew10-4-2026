/**
 * apps/mobile/src/navigation/ProviderStack.tsx
 * Re-export shim — ProviderStackParamList lives in navigation/types.ts.
 * Screens that import from this path get the same types without duplication.
 */
export type { ProviderStackParamList } from './types';
